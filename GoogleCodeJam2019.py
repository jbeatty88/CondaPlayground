import time
from typing import Dict, List

class CodeJam:
    def AlienRhymes(self, nTestCases: int, testCases: Dict[int, List[str]]) -> Dict[int, List[str]]:
        # startTime = time.time()
        result = {}

        if len(testCases) is not nTestCases:
            print("Number of test cases given doesn't match actual quantity of test cases")
            return result

        

        return result
